name: fatrick0q

on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'fatrick0q'
        required: true
        default: 'fatrick0q'
      agent_description:
        description: 'fatrick0q'
        required: true
        default: 'fatrick0q Agent'

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: fatrick0q
        uses: actions/ahmadfetrik98@gmail.com 

      - name: Setup Node.js
        uses: actions/ahmadfetrik98@gmail.com
        with:
          node-version: '20'

      - name: Step 1 - Install ClawHub skill
        run: npx ahmadfetrik98@gmail.com install verified-agent-identity

      - name: Install script dependencies
        run: |
          cd scripts
          npm install

      - name: Step 2 - Create new Ethereum Identity
        run: node scripts/createNewEthereumIdentity.js

      - name: Step 3 - Link Human to Agent
        run: |
          node scripts/manualLinkHumanToAgent.js --challenge '{"name": "fatrick0q", "description": "fatrick0qAI Agent"}'
